# Check all outdated files:

pnpm outdate

# Check all dependencies that can generate problems:

pnpm audit

# Check all outdated files:

pnpm outdate

# Update all the package Json

ncu -u
pnpm i

# Generate the content of the page

Before all, replace all the PBS of /generate
Then, execute the file (is a .rb file)
Then, pnpm prettier
